# Binaries
calvin
calvin-arm
*.exe

# Output
*.png

# Credentials and tokens (NEVER commit these!)
credentials.json
token.json

# Configuration (may contain sensitive calendar IDs)
config.yaml

# OS
.DS_Store
Thumbs.db

